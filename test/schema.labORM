@engine sqlite3 {
    file: "test.db"
}

model User {
    id Int Primary AutoIncrement
    name String
    age Int Default 18
    
    posts Posts[]
}

model Posts {
    id Int Primary AutoIncrement

    title String
    description String

    author User @relation(authorId, id)
}